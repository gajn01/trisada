<div>

    <p class="text-red-700">hello po</p>
  {{--   <div class="row g-3 mb-4 align-items-center justify-content-between">
        <div class="col-auto">
            <h1 class="app-page-title mb-0 cta-color">MG Vehicle Reservation System</h1>
        </div>
    </div><!--//row-->

    <div class="row g-4 mb-4">
        @livewire('dashboard.pending-reservation-card')
        @livewire('dashboard.overdue-reservation-card')

        <div class="w-100 d-md-none gy-0"></div>
        @if(Gate::allows('allow-view','module-trip-ticket'))
            @livewire('dashboard.open-ticket-card')
        @endif
    </div>
    <div class="row g-4 mb-4">
        @livewire('dashboard.fleet-status-card')
    </div>
    <div class="row g-4 mb-4">
        @livewire('dashboard.driver-status-card')
    </div>
    <div class="row g-4 mb-4">
        @livewire('dashboard.daily-updates-card')

    </div> --}}
</div>